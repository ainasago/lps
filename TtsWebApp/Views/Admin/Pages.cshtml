@model List<TtsWebApp.Models.Article>
@{
    ViewData["Title"] = "页面管理"; 
}

<div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">页面管理</h1>
</div>

<div class="bg-white rounded-lg shadow overflow-hidden">
    <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
                <tr>
                    <th class="px-3 md:px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">页面标题</th>
                    <th class="px-3 md:px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider hidden sm:table-cell">标识符</th>
                    <th class="px-3 md:px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider hidden lg:table-cell">更新时间</th>
                    <th class="px-3 md:px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">操作</th>
                </tr>
            </thead>
        <tbody class="bg-white divide-y divide-gray-200">
            @foreach (var item in Model)
            {
                <tr class="hover:bg-gray-50">
                    <td class="px-3 md:px-6 py-3 md:py-4">
                        <div class="text-xs md:text-sm font-medium text-gray-900">@item.Title</div>
                    </td>
                    <td class="px-3 md:px-6 py-3 md:py-4 whitespace-nowrap text-xs md:text-sm text-gray-500 hidden sm:table-cell">
                        <code class="bg-gray-100 px-1.5 md:px-2 py-1 rounded text-xs">@item.Slug</code>
                    </td>
                    <td class="px-3 md:px-6 py-3 md:py-4 whitespace-nowrap text-xs md:text-sm text-gray-500 hidden lg:table-cell">
                        @item.UpdatedAt.ToString("yyyy-MM-dd HH:mm")
                    </td>
                    <td class="px-3 md:px-6 py-3 md:py-4 whitespace-nowrap text-xs md:text-sm">
                        <a asp-action="EditPage" asp-route-id="@item.Id" 
                           class="inline-flex items-center px-2 md:px-3 py-1 md:py-1.5 bg-blue-600 hover:bg-blue-700 text-white rounded text-xs md:text-sm transition"
                           title="编辑页面">
                            <span class="material-symbols-outlined text-sm md:text-base mr-1">edit</span>
                            <span class="hidden sm:inline">编辑</span>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
        </table>
    </div>
</div>

<div class="mt-6 bg-blue-50 border border-blue-200 rounded-lg p-4">
    <div class="flex items-start">
        <span class="material-symbols-outlined text-blue-600 mr-2">info</span>
        <div class="text-sm text-blue-800">
            <p class="font-bold mb-1">说明</p>
            <p>这些页面对应前台的固定页面（如关于我们、隐私政策等）。修改内容后，前台页面会立即更新。</p>
            <p class="mt-2">页面标识符（Slug）不可修改，用于系统识别页面。</p>
        </div>
    </div>
</div>
