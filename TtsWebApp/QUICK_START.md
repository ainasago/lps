# 🚀 JavaScript 混淆快速开始

## 一分钟上手

### 1. 安装依赖（首次使用）

```bash
cd TtsWebApp
npm install
```

### 2. 选择混淆模式

#### 🧪 测试混淆效果（安全）

```bash
npm run obfuscate
```

✅ 生成混淆文件到 `wwwroot/js/obfuscated/`  
✅ **不会修改原文件**  
✅ 可以查看混淆效果

#### 🚀 发布项目（一键完成）

```bash
npm run publish
```

✅ 自动混淆所有 JS 文件  
✅ 自动备份原文件  
✅ 自动执行 `dotnet publish`  
✅ 生成发布包

#### 🔄 恢复原文件

```bash
npm run restore
```

✅ 从备份恢复原文件  
✅ 继续开发

## 📋 完整工作流

### 开发阶段

```bash
# 正常开发，无需混淆
dotnet run
```

### 发布阶段

```bash
# 方式一：一键发布（推荐）
npm run publish

# 方式二：分步执行
npm run obfuscate:prod    # 混淆
dotnet publish -c Release # 发布
npm run restore           # 恢复原文件
```

## ⚠️ 重要提示

1. **生产混淆会替换原文件**（但会自动备份）
2. **发布前建议先提交代码到 Git**
3. **混淆后的代码无法调试**
4. **开发时使用原文件，发布时才混淆**

## 📊 混淆效果

### 原始代码

```javascript
function convertText() {
    const text = document.getElementById('inputText').value;
    if (!text) {
        alert('请输入文本');
        return;
    }
    // ...
}
```

### 混淆后

```javascript
var _0x4a2b=['getElementById','value','inputText','请输入文本','alert'];(function(_0x2d8f05,_0x4b81bb){var _0x4d74cb=function(_0x32719f){while(--_0x32719f){_0x2d8f05['push'](_0x2d8f05['shift']());}};_0x4d74cb(++_0x4b81bb);}(_0x4a2b,0x1f4));var _0x4d74=function(_0x2d8f05,_0x4b81bb){_0x2d8f05=_0x2d8f05-0x0;var _0x4d74cb=_0x4a2b[_0x2d8f05];return _0x4d74cb;};function convertText(){const _0x3a7c8b=document[_0x4d74('0x0')](_0x4d74('0x2'))[_0x4d74('0x1')];if(!_0x3a7c8b){alert(_0x4d74('0x4'));return;}}
```

**效果**：
- ✅ 变量名完全混淆
- ✅ 字符串加密编码
- ✅ 控制流打乱
- ✅ 添加反调试代码
- ✅ 几乎不可读

## 🛠️ 故障排除

### 问题：npm install 失败

```bash
# 清除缓存重试
npm cache clean --force
npm install
```

### 问题：混淆后代码不工作

```bash
# 恢复原文件
npm run restore

# 检查代码是否使用了 eval() 或反射
```

### 问题：想要更强/更弱的混淆

编辑 `obfuscate-production.js` 中的配置选项

## 📚 详细文档

查看 [OBFUSCATION_README.md](./OBFUSCATION_README.md) 了解：
- 详细配置说明
- 混淆选项调整
- 性能优化建议
- 常见问题解答

---

**提示**：混淆是代码保护的一部分，核心业务逻辑应该放在服务器端！
